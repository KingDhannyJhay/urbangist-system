{
  "name": "urbangist-worker",
  "version": "1.0.0",
  "private": true,
  "description": "UrbanGist background worker — FFmpeg processing + cron jobs (Railway)",
  "main": "dist/worker.js",
  "scripts": {
    "dev":   "tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/worker.js",
    "lint":  "eslint src/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.44.4",
    "dotenv":               "^16.4.5",
    "fluent-ffmpeg":        "^2.1.3"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg":  "^2.1.24",
    "@types/node":           "^20.14.2",
    "typescript":            "^5.4.5",
    "tsx":                   "^4.15.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
